import { ProductList } from "../../Components/ProductList/ProductList"

export const CartPage =()=>{
    return <div>
        <h2 style={{ visibility: "hidden", display: "none" }}>Cart page</h2>
        <ProductList/>
    </div>

}